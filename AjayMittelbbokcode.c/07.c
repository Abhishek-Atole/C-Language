//A program to find the size of variable of various data types
#include<stdio.h>
int main()
{
    printf("Char takes %d bytes in the memory\n",sizeof(char));
    printf("Integer takes %d bytes in the memory\n",sizeof(int));
    printf("Float takes %d bytes in mamory \n",sizeof(float));
    printf("Long takes %d bytes in mamory \n",sizeof(long));
    printf("Double takes %d bytes in mamory \n",sizeof(double));
    return 0;
}
